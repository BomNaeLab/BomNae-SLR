import md_pipe_interface as proc
import trained_model as model
# the main api request handling script


# handle inbounding request

# process image data using proc (mind frame rate and timing)

# feed it to the model

# return response